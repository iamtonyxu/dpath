# 最小二乘对角加载（Ridge / Tikhonov）与 λ 的选取

本文说明最小二乘模型求解中"坏矩阵"的成因、对角线加微扰（对角加载）的原理，
以及如何实际选取正则化参数 λ。配套示例脚本：`ls_diagonal_loading.m`。

---

## 1. 问题：病态的法方程

最小二乘的法方程形式为

$$
\theta = (A^T A)^{-1} A^T b
$$

两个关键事实：

1. **条件数被平方**：$\kappa(A^T A) = \kappa(A)^2$。即使 $A$ 只是中等病态
   （如 $\kappa(A)=10^4$），$A^TA$ 已经是 $10^8$。
2. **"正定判别"只验证定性，不验证定量**：`chol` 成功、`min(eig) > 0` 只能说明
   矩阵是正定的，从不说明它是否良态。一个正定矩阵完全可以是
   $\kappa = 10^{16}$ —— 判别通过，但解的相对误差被放大 $10^{16}$ 倍，照样发散。

> 结论：**正定 ≠ 良态**。发散的原因是最小特征值 $\sigma_{\min}^2$ 落在了噪声底
> 之下，噪声沿该弱方向被放大 $1/\sigma_{\min}^2$。

---

## 2. 解法：对角线加微扰 λI

把法方程改成

$$
(A^T A + \lambda I)\,\theta = A^T b, \qquad \lambda > 0
$$

这一招在不同领域有不同名字：**Ridge 回归**（统计）、**Tikhonov 正则化**（数值）、
**对角加载**（雷达/自适应滤波）、**Levenberg–Marquardt 阻尼**（非线性最小二乘）。

**谱解释**：每个特征值 $\sigma_i^2$ 被抬到 $\sigma_i^2 + \lambda$，条件数变为

$$
\kappa(A^TA+\lambda I)=\frac{\sigma_{\max}^2+\lambda}{\sigma_{\min}^2+\lambda}
\le \frac{\sigma_{\max}^2+\lambda}{\lambda}
$$

那些落在噪声底之下的小特征值被抬到 $\lambda$ 之上，噪声放大倍数被钳制在
$1/\lambda$。

**等价形式**：对角加载等价于求解带能量惩罚的优化问题

$$
\min_\theta\ \|A\theta-b\|^2 + \lambda\,\|\theta\|^2
$$

即"允许贴数据，但不许系数太大"，用一点偏差换取方差的大幅下降。

---

## 3. λ 是什么（直白理解）

把 λ 想成一个**"惩罚旋钮"**，它决定"你允许多大的系数"。

- **λ = 0**：不惩罚，模型拼命贴数据 → 把噪声也当信号抓进来 → 系数爆炸、发散。
- **λ = ∞**：惩罚到最大，模型只能给全 0 → 什么都不拟合 → 彻底没用。
- **中间**：恰到好处，允许模型抓住大的真实趋势，但摁住细小、由噪声引起的抖动。

因此选 λ，本质是**划一条"信号 / 噪声"的分界线**：数据里多小的波动可以被认定是
噪声、不去追？

- 线划太低 → 噪声漏进来 → 过拟合、发散；
- 线划太高 → 真信号也被当噪声摁掉 → 欠拟合、有偏。

---

## 4. λ 的选取（实操方法）

选 λ 不需要知道真值 $\theta_{\text{true}}$。以下是三种常用方法。

### 4.1 交叉验证（Cross-Validation）

最直观、最常用。

- 把数据分成**训练集**（用来算系数）和**验证集**（用来考模型）；
- 对每个候选 λ：在训练集上求解，在**没见过的验证集**上算预测误差；
- 取验证误差最小的 λ。

直觉：噪声不会在两次数据里重复出现，所以能贴住"没见过的数据"的模型，才说明它
抓住的是真信号。小样本时用 **k 折交叉验证**：把数据分 k 份，轮流留一份做验证，
对 k 次结果取平均，更稳。

### 4.2 L 曲线（L-Curve）

不需要额外切分数据，一张图找拐角。

对每个 λ 记两个量：

- 拟合残差 $\|A\theta(\lambda)-b\|$（贴得多好）；
- 解范数 $\|\theta(\lambda)\|$（系数多大）。

以双对数坐标画出两者的关系，形状呈一个 **L**。**拐角处就是甜点**：

- 拐角左侧：系数暴涨，但残差几乎不再下降 → 那部分增长全是在追噪声，白涨；
- 拐角右侧：残差显著上升，系数却不再变小 → 白丢信号。

### 4.3 已知噪声水平（Discrepancy 原则）

若噪声方差 $\sigma^2$ 大致已知，选 λ 使残差满足

$$
\|A\theta(\lambda)-b\|^2 \approx N\sigma^2
$$

即"别让模型贴得比噪声本身还准"——再准就是在拟合噪声。这是最简单、物理含义最
清楚的方法，前提是噪声水平可估计。

### 4.4 GCV（广义交叉验证）

交叉验证的一个近似：不需要真的切分数据、反复求解，从一次求解就能算出留一法
(leave-one-out) 的交叉验证分数，取 GCV 分数最小的 λ。适合数据量小、又嫌
k 折计算开销大的场景。

### 方法对比

| 方法 | 需要切分数据？ | 需要知道噪声？ | 计算开销 | 适用场景 |
|------|:---:|:---:|:---:|------|
| 交叉验证 | 是 | 否 | 高（多次求解） | 通用、最稳 |
| L 曲线 | 否 | 否 | 中（画图判读） | 定性/人工分析 |
| Discrepancy 原则 | 否 | 是 | 低 | 噪声水平可估计 |
| GCV | 否 | 否 | 低 | 小样本、批量自动化 |

---

## 5. 示例与参考

脚本 `ls_diagonal_loading.m` 用 15 阶单项式基构造病态问题，实测：

| 方法 | $\|\theta\|$ | $\|\theta-\theta_{\text{true}}\|$ |
|------|------------:|----------------------------------:|
| 真实参数 | 0.276 | 0 |
| 法方程 LS（无正则） | 4415 | 4415 |
| QR `A\b`（无正则） | 1.04×10⁶ | 1.04×10⁶ |
| **对角加载 @ λ=3.2×10⁻⁴** | 0.279 | **0.0203** |

注意：脚本里"最优 λ"是按参数误差取的，**仅为演示**；工程中拿不到
$\theta_{\text{true}}$，应改用上面的交叉验证 / L 曲线 / GCV / 噪声水平方法。

---

## 6. 一句话总结

λ 是"你信多少细节"的旋钮，选它就是选"信号和噪声的分界"；交叉验证、L 曲线、
GCV、discrepancy 原则，都是把这个分界找出来的工具。
